apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus-server
  namespace: default
  labels:
    app.kubernetes.io/instance: prometheus
    app.kubernetes.io/name: prometheus-server
spec:
  hostnames:
    - prometheus.ydphoto.com
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: yd-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: prometheus-server
          port: 80
          weight: 1